package com.itacademy;

public final class TrolleyBus extends Bus {
    public TrolleyBus(String model, int height, int weight, int maxSpeed, String route) {
        super(model, height, weight, maxSpeed, route);
    }

    public TrolleyBus(String model) {
        super(model);
    }
}
